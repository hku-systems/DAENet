//
// Created by jianyu on 6/9/19.
//

#include <string>

/*
 * daemonize.c
 * This example daemonizes a process, writes a few log messages,
 * sleeps 20 seconds and terminates afterwards.
 */

void run_as_daemon(std::string host, int port);